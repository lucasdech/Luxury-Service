<form method="post" action="{{ path('app_job_to_candidat_delete', {'id': job_to_candidat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_to_candidat.id) }}">
    <button class="btn">Delete</button>
</form>
