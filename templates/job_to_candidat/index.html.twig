{% extends 'base.html.twig' %}

{% block title %}JobToCandidat index{% endblock %}

{% block body %}
    <h1>JobToCandidat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Time</th>
                <th>Is_approved</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for job_to_candidat in job_to_candidats %}
            <tr>
                <td>{{ job_to_candidat.id }}</td>
                <td>{{ job_to_candidat.time ? job_to_candidat.time|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ job_to_candidat.isApproved ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_job_to_candidat_show', {'id': job_to_candidat.id}) }}">show</a>
                    <a href="{{ path('app_job_to_candidat_edit', {'id': job_to_candidat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_job_to_candidat_new') }}">Create new</a>
{% endblock %}
